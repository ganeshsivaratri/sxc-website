<section class="experience-section" id="sxc-experience">
  <div class="experience-bg-grid"></div>
  <div class="experience-glow glow-one"></div>
  <div class="experience-glow glow-two"></div>

  <div class="experience-container">
    <!-- Section Header -->
    <div class="section-header reveal">
      <span class="eyebrow">THE SXC EXPERIENCE</span>

      <h2>
        Learn Through<br />
        <span>Real Experience.</span>
      </h2>

      <p>
        SXC brings together practical learning, collaborative execution, and
        real product-building experiences in a startup-oriented environment.
      </p>
    </div>

    <!-- Hybrid Visual Layout -->
    <div class="experience-layout">
      <!-- Main Visual Panel -->
      <div class="experience-visual reveal">
        <div class="visual-label">THE SXC MODEL</div>

        <div class="visual-content">
          <div class="orbit orbit-large"></div>
          <div class="orbit orbit-small"></div>

          <div class="central-panel">
            <div class="central-panel-top">
              <span class="status-dot"></span>
              <span>ACTIVE LEARNING SYSTEM</span>
            </div>

            <div class="central-title">SXC</div>

            <p>
              From learning concepts to building meaningful products.
            </p>

            <div class="central-progress">
              <span></span>
              <span></span>
              <span></span>
              <span></span>
            </div>
          </div>

          <div class="floating-panel panel-top">
            <small>01 / EXPOSURE</small>
            <strong>Learn by doing</strong>
          </div>

          <div class="floating-panel panel-bottom">
            <small>02 / EXECUTION</small>
            <strong>Build with teams</strong>
          </div>
        </div>

        <div class="visual-footer">
          <span>PRODUCT DEVELOPMENT</span>
          <span>COLLABORATION</span>
          <span>PORTFOLIO</span>
        </div>
      </div>

      <!-- Experience Pillars -->
      <div class="experience-pillars">
        <article class="pillar-card reveal">
          <div class="pillar-heading">
            <span class="pillar-index">01</span>
            <h3>Practical Learning</h3>
          </div>

          <p>
            Convert academic knowledge into practical skills through
            real-world product-development tasks.
          </p>

          <div class="pillar-line">
            <span></span>
          </div>
        </article>

        <article class="pillar-card reveal">
          <div class="pillar-heading">
            <span class="pillar-index">02</span>
            <h3>Collaborative Building</h3>
          </div>

          <p>
            Work with team members, exchange ideas, and contribute to shared
            product goals.
          </p>

          <div class="pillar-line">
            <span></span>
          </div>
        </article>

        <article class="pillar-card reveal">
          <div class="pillar-heading">
            <span class="pillar-index">03</span>
            <h3>Ownership & Responsibility</h3>
          </div>

          <p>
            Take responsibility for assigned work and understand how
            individual contributions support a larger product.
          </p>

          <div class="pillar-line">
            <span></span>
          </div>
        </article>

        <article class="pillar-card reveal">
          <div class="pillar-heading">
            <span class="pillar-index">04</span>
            <h3>Portfolio Development</h3>
          </div>

          <p>
            Build meaningful projects that demonstrate your skills,
            contributions, and growth.
          </p>

          <div class="pillar-line">
            <span></span>
          </div>
        </article>
      </div>
    </div>

    <!-- Learning Journey -->
    <div class="journey-section reveal">
      <div class="journey-heading">
        <span class="eyebrow">THE JOURNEY</span>

        <h3>
          From <span>Idea</span> to <span>Execution.</span>
        </h3>
      </div>

      <div class="journey-track">
        <div class="journey-progress"></div>

        <div class="journey-step">
          <span class="journey-number">01</span>
          <div>
            <h4>Understand</h4>
            <p>Explore concepts, challenges, and project requirements.</p>
          </div>
        </div>

        <div class="journey-step">
          <span class="journey-number">02</span>
          <div>
            <h4>Collaborate</h4>
            <p>Share ideas and work with a project-focused team.</p>
          </div>
        </div>

        <div class="journey-step">
          <span class="journey-number">03</span>
          <div>
            <h4>Build</h4>
            <p>Transform ideas into functional project outcomes.</p>
          </div>
        </div>

        <div class="journey-step">
          <span class="journey-number">04</span>
          <div>
            <h4>Showcase</h4>
            <p>Present your work and document your contributions.</p>
          </div>
        </div>
      </div>
    </div>

    <!-- Closing Statement -->
    <div class="experience-footer reveal">
      <strong>
        Learn by building. Grow through collaboration. Show your work.
      </strong>
    </div>
  </div>
</section>

<style>
  .experience-section {
    position: relative;
    isolation: isolate;
    overflow: hidden;
    padding: 120px 6%;
    background: #ffffff;
    color: #081f33;
  }

  .experience-container {
    position: relative;
    z-index: 2;
    max-width: 1450px;
    margin: 0 auto;
  }

  .experience-bg-grid {
    position: absolute;
    inset: 0;
    opacity: 0.4;
    pointer-events: none;
    background-image:
      linear-gradient(rgba(8, 31, 51, 0.035) 1px, transparent 1px),
      linear-gradient(90deg, rgba(8, 31, 51, 0.035) 1px, transparent 1px);
    background-size: 45px 45px;
    mask-image: linear-gradient(to bottom, black, transparent 90%);
  }

  .experience-glow {
    position: absolute;
    width: 420px;
    height: 420px;
    border-radius: 50%;
    filter: blur(100px);
    pointer-events: none;
    opacity: 0.12;
  }

  .glow-one {
    top: 8%;
    right: -180px;
    background: #4de4ff;
  }

  .glow-two {
    bottom: 8%;
    left: -200px;
    background: #087ea4;
  }

  .section-header {
    max-width: 760px;
    margin-bottom: 65px;
  }

  .eyebrow {
    color: #087ea4;
    font-size: 0.78rem;
    font-weight: 800;
    letter-spacing: 0.18em;
  }

  h2 {
    margin: 18px 0;
    font-size: clamp(2.8rem, 5vw, 5rem);
    line-height: 1.02;
    letter-spacing: -0.05em;
  }

  h2 span,
  .journey-heading span {
    color: #087ea4;
  }

  .section-header p {
    max-width: 680px;
    color: #657786;
    font-size: 1.05rem;
    line-height: 1.8;
  }

  /* Main Hybrid Layout */
  .experience-layout {
    display: grid;
    grid-template-columns: 1.05fr 1fr;
    gap: 28px;
    align-items: stretch;
  }

  /* Visual Panel */
  .experience-visual {
    position: relative;
    min-height: 580px;
    overflow: hidden;
    border: 1px solid #dceaf0;
    border-radius: 30px;
    background:
      linear-gradient(
        145deg,
        rgba(77, 228, 255, 0.12),
        transparent 40%
      ),
      #f8fcfe;
    box-shadow: 0 20px 70px rgba(8, 31, 51, 0.07);
  }

  .visual-label {
    position: absolute;
    top: 28px;
    left: 30px;
    color: #087ea4;
    font-size: 0.72rem;
    font-weight: 800;
    letter-spacing: 0.15em;
  }

  .visual-content {
    position: absolute;
    inset: 90px 30px 100px;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .orbit {
    position: absolute;
    border: 1px solid rgba(8, 126, 164, 0.18);
    border-radius: 50%;
    animation: orbitRotate 22s linear infinite;
  }

  .orbit-large {
    width: 390px;
    height: 390px;
  }

  .orbit-small {
    width: 275px;
    height: 275px;
    border-style: dashed;
    animation-duration: 16s;
    animation-direction: reverse;
  }

  .central-panel {
    position: relative;
    z-index: 2;
    width: 270px;
    padding: 28px;
    border: 1px solid rgba(77, 228, 255, 0.5);
    border-radius: 24px;
    background: rgba(255, 255, 255, 0.9);
    box-shadow:
      0 20px 50px rgba(8, 31, 51, 0.1),
      inset 0 0 35px rgba(77, 228, 255, 0.08);
    backdrop-filter: blur(16px);
    animation: panelFloat 5s ease-in-out infinite;
  }

  .central-panel-top {
    display: flex;
    align-items: center;
    gap: 8px;
    color: #7c929f;
    font-size: 0.58rem;
    font-weight: 800;
    letter-spacing: 0.08em;
  }

  .status-dot {
    width: 7px;
    height: 7px;
    border-radius: 50%;
    background: #25b99a;
    box-shadow: 0 0 12px rgba(37, 185, 154, 0.6);
  }

  .central-title {
    margin: 30px 0 12px;
    color: #081f33;
    font-size: 4.8rem;
    font-weight: 900;
    letter-spacing: -0.1em;
  }

  .central-panel p {
    color: #657786;
    font-size: 0.9rem;
    line-height: 1.6;
  }

  .central-progress {
    display: flex;
    gap: 6px;
    margin-top: 25px;
  }

  .central-progress span {
    height: 5px;
    flex: 1;
    border-radius: 10px;
    background: #4de4ff;
    animation: progressPulse 2s ease-in-out infinite;
  }

  .central-progress span:nth-child(2) {
    animation-delay: 0.2s;
  }

  .central-progress span:nth-child(3) {
    animation-delay: 0.4s;
  }

  .central-progress span:nth-child(4) {
    animation-delay: 0.6s;
  }

  .floating-panel {
    position: absolute;
    z-index: 3;
    display: flex;
    flex-direction: column;
    gap: 8px;
    padding: 18px 20px;
    border: 1px solid #dceaf0;
    border-radius: 15px;
    background: rgba(255, 255, 255, 0.8);
    box-shadow: 0 12px 30px rgba(8, 31, 51, 0.07);
    backdrop-filter: blur(12px);
  }

  .floating-panel small {
    color: #087ea4;
    font-size: 0.62rem;
    font-weight: 800;
  }

  .floating-panel strong {
    color: #081f33;
    font-size: 0.85rem;
  }

  .panel-top {
    top: 5%;
    right: 0;
    animation: panelFloat 5s ease-in-out infinite 0.8s;
  }

  .panel-bottom {
    bottom: 3%;
    left: 0;
    animation: panelFloat 5s ease-in-out infinite 1.5s;
  }

  .visual-footer {
    position: absolute;
    right: 30px;
    bottom: 28px;
    left: 30px;
    display: flex;
    justify-content: space-between;
    gap: 15px;
    color: #8aa0ac;
    font-size: 0.58rem;
    font-weight: 800;
    letter-spacing: 0.08em;
  }

  /* Pillar Cards */
  .experience-pillars {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 18px;
  }

  .pillar-card {
    position: relative;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    padding: 28px;
    border: 1px solid #e1ebf0;
    border-radius: 22px;
    background: rgba(255, 255, 255, 0.85);
    box-shadow: 0 8px 25px rgba(8, 31, 51, 0.035);
    transition:
      transform 0.4s ease,
      border-color 0.4s ease,
      box-shadow 0.4s ease;
  }

  .pillar-card:hover {
    transform: translateY(-7px);
    border-color: rgba(8, 126, 164, 0.4);
    box-shadow: 0 18px 40px rgba(8, 31, 51, 0.09);
  }

  .pillar-heading {
    display: flex;
    flex-direction: column;
    gap: 22px;
  }

  .pillar-index {
    color: #087ea4;
    font-size: 0.75rem;
    font-weight: 800;
    letter-spacing: 0.12em;
  }

  .pillar-card h3 {
    margin: 0;
    color: #081f33;
    font-size: 1.2rem;
    line-height: 1.25;
  }

  .pillar-card p {
    color: #657786;
    font-size: 0.9rem;
    line-height: 1.7;
  }

  .pillar-line {
    height: 3px;
    margin-top: 25px;
    overflow: hidden;
    border-radius: 10px;
    background: #edf4f7;
  }

  .pillar-line span {
    display: block;
    width: 35%;
    height: 100%;
    border-radius: inherit;
    background: linear-gradient(90deg, #4de4ff, #087ea4);
    transition: width 0.5s ease;
  }

  .pillar-card:hover .pillar-line span {
    width: 100%;
  }

  /* Journey Section */
  .journey-section {
    margin-top: 90px;
    padding: 45px;
    border: 1px solid #e1ebf0;
    border-radius: 28px;
    background: #f8fcfe;
  }

  .journey-heading h3 {
    margin: 15px 0 40px;
    font-size: clamp(1.8rem, 3vw, 2.8rem);
    letter-spacing: -0.04em;
  }

  .journey-track {
    position: relative;
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 25px;
  }

  .journey-progress {
    position: absolute;
    top: 23px;
    right: 10%;
    left: 10%;
    height: 1px;
    background: linear-gradient(90deg, #4de4ff, #087ea4);
    opacity: 0.4;
  }

  .journey-step {
    position: relative;
    z-index: 1;
  }

  .journey-number {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 47px;
    height: 47px;
    margin-bottom: 25px;
    border: 1px solid #b9dce7;
    border-radius: 50%;
    background: #ffffff;
    color: #087ea4;
    font-size: 0.8rem;
    font-weight: 800;
    box-shadow: 0 0 0 8px #f8fcfe;
  }

  .journey-step h4 {
    margin: 0 0 12px;
    color: #081f33;
    font-size: 1.1rem;
  }

  .journey-step p {
    max-width: 230px;
    margin: 0;
    color: #657786;
    font-size: 0.88rem;
    line-height: 1.7;
  }

  .experience-footer {
    margin-top: 60px;
    text-align: center;
  }

  .experience-footer strong {
    color: #087ea4;
    font-size: clamp(1rem, 2vw, 1.25rem);
  }

  /* Reveal Animation */
  .reveal {
    opacity: 0;
    transform: translateY(35px);
    transition:
      opacity 0.8s ease,
      transform 0.8s ease;
  }

  .reveal.visible {
    opacity: 1;
    transform: translateY(0);
  }

  .pillar-card:nth-child(2) {
    transition-delay: 0.1s;
  }

  .pillar-card:nth-child(3) {
    transition-delay: 0.2s;
  }

  .pillar-card:nth-child(4) {
    transition-delay: 0.3s;
  }

  @keyframes panelFloat {
    0%,
    100% {
      transform: translateY(0);
    }

    50% {
      transform: translateY(-10px);
    }
  }

  @keyframes orbitRotate {
    from {
      transform: rotate(0deg);
    }

    to {
      transform: rotate(360deg);
    }
  }

  @keyframes progressPulse {
    0%,
    100% {
      opacity: 0.35;
    }

    50% {
      opacity: 1;
    }
  }

  @media (max-width: 1100px) {
    .experience-layout {
      grid-template-columns: 1fr;
    }

    .experience-visual {
      min-height: 520px;
    }
  }

  @media (max-width: 800px) {
    .experience-section {
      padding: 85px 6%;
    }

    .experience-pillars {
      grid-template-columns: 1fr 1fr;
    }

    .journey-section {
      padding: 30px;
    }

    .journey-track {
      grid-template-columns: 1fr 1fr;
      gap: 35px 25px;
    }

    .journey-progress {
      display: none;
    }
  }

  @media (max-width: 550px) {
    .experience-pillars {
      grid-template-columns: 1fr;
    }

    .experience-visual {
      min-height: 470px;
    }

    .visual-content {
      inset: 85px 10px 90px;
    }

    .orbit-large {
      width: 300px;
      height: 300px;
    }

    .orbit-small {
      width: 220px;
      height: 220px;
    }

    .central-panel {
      width: 230px;
      padding: 23px;
    }

    .central-title {
      font-size: 4rem;
    }

    .floating-panel {
      padding: 13px;
    }

    .panel-top {
      right: -5px;
    }

    .panel-bottom {
      left: -5px;
    }

    .visual-footer {
      flex-wrap: wrap;
      font-size: 0.5rem;
    }

    .journey-track {
      grid-template-columns: 1fr;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    *,
    *::before,
    *::after {
      animation-duration: 0.01ms !important;
      animation-iteration-count: 1 !important;
      transition-duration: 0.01ms !important;
    }

    .reveal {
      opacity: 1;
      transform: none;
    }
  }
</style>

<script>
  const revealElements = document.querySelectorAll(".reveal");

  const revealObserver = new IntersectionObserver(
    (entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          entry.target.classList.add("visible");
          revealObserver.unobserve(entry.target);
        }
      });
    },
    {
      threshold: 0.15,
    }
  );

  revealElements.forEach((element) => {
    revealObserver.observe(element);
  });
</script>