<section class="why-join" id="why-join">
  <div class="section-container">
    <!-- Full-width heading -->
    <div class="section-heading">
      <span class="eyebrow">WHY JOIN SXC</span>

      <h2>
        Build Experience That
        <span>Goes Beyond the Classroom.</span>
      </h2>

      <p>
        Turn your knowledge into practical experience by working on projects,
        collaborating with teams, and contributing to product development.
      </p>
    </div>

    <!-- Main content -->
    <div class="content-grid">
      <!-- Visual panel -->
      <div class="experience-visual">
        <div class="orbit orbit-one"></div>
        <div class="orbit orbit-two"></div>
        <div class="orbit orbit-three"></div>

        <div class="visual-card">
          <span class="visual-label">SXC</span>

          <h3>
            BUILD<br />
            YOUR<br />
            EXPERIENCE
          </h3>

          <div class="visual-line"></div>

          <span class="visual-caption">REAL CONTRIBUTION</span>
        </div>

        <div class="floating-tag tag-one">REAL PROJECTS</div>
        <div class="floating-tag tag-two">TEAMWORK</div>
        <div class="floating-tag tag-three">REAL EXPERIENCE</div>
      </div>

      <!-- Benefits grid -->
      <div class="benefits-grid">
        <article class="benefit-card">
          <span class="card-number">01</span>
          <h3>Real Project Experience</h3>
          <p>
            Work on practical projects and understand how real products are
            developed.
          </p>
        </article>

        <article class="benefit-card">
          <span class="card-number">02</span>
          <h3>Collaborative Learning</h3>
          <p>
            Learn from teammates while sharing ideas, skills, and
            responsibilities.
          </p>
        </article>

        <article class="benefit-card">
          <span class="card-number">03</span>
          <h3>Portfolio Building</h3>
          <p>
            Create meaningful work that demonstrates your skills and
            contributions.
          </p>
        </article>

        <article class="benefit-card">
          <span class="card-number">04</span>
          <h3>Professional Growth</h3>
          <p>
            Strengthen your communication, teamwork, and problem-solving
            abilities.
          </p>
        </article>

        <article class="benefit-card">
          <span class="card-number">05</span>
          <h3>SXC Certificate</h3>
          <p>
            Receive an SXC certificate upon completing your approved project
            journey.
          </p>
        </article>

        <article class="benefit-card">
          <span class="card-number">06</span>
          <h3>Startup Exposure</h3>
          <p>
            Experience a startup-oriented environment without a promise of
            employment or salary.
          </p>
        </article>
      </div>
    </div>

    
  </div>
</section>

<style>
  .why-join {
    width: 100%;
    padding: 120px 6% 110px;
    background: #071a2b;
    color: #ffffff;
    overflow: hidden;
  }

  .section-container {
    width: 100%;
    max-width: 1380px;
    margin: 0 auto;
  }

  /* Full-width heading */

  .section-heading {
    width: 100%;
    max-width: 1050px;
    margin: 0 auto 75px;
    text-align: center;
  }

  .eyebrow {
    display: inline-block;
    margin-bottom: 24px;
    color: #4de4ff;
    font-size: 0.85rem;
    font-weight: 800;
    letter-spacing: 0.2em;
  }

  .section-heading h2 {
    margin: 0;
    font-size: clamp(2.8rem, 5vw, 5.2rem);
    font-weight: 800;
    line-height: 1.03;
    letter-spacing: -0.055em;
  }

  .section-heading h2 span {
    display: block;
    color: #4de4ff;
  }

  .section-heading p {
    max-width: 850px;
    margin: 28px auto 0;
    color: #a8bac9;
    font-size: 1.12rem;
    line-height: 1.8;
  }

  /* Main layout */

  .content-grid {
    display: grid;
    grid-template-columns: 0.9fr 1.5fr;
    gap: 65px;
    align-items: center;
  }

  /* Visual panel */

  .experience-visual {
    position: relative;
    min-height: 570px;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .orbit {
    position: absolute;
    border: 1px solid rgba(77, 228, 255, 0.24);
    border-radius: 50%;
    transform: rotate(-25deg);
  }

  .orbit-one {
    width: 440px;
    height: 280px;
  }

  .orbit-two {
    width: 500px;
    height: 330px;
  }

  .orbit-three {
    width: 370px;
    height: 370px;
    transform: rotate(35deg);
  }

  .visual-card {
    position: relative;
    z-index: 2;
    width: 285px;
    height: 350px;
    padding: 55px 38px;
    border: 1px solid rgba(77, 228, 255, 0.55);
    border-radius: 50%;
    background:
      radial-gradient(
        circle at center,
        rgba(77, 228, 255, 0.15),
        transparent 70%
      ),
      #0a2639;
    box-shadow: 0 0 70px rgba(77, 228, 255, 0.08);
  }

  .visual-label {
    color: #4de4ff;
    font-size: 1.1rem;
    font-weight: 800;
    letter-spacing: 0.15em;
  }

  .visual-card h3 {
    margin: 28px 0 0;
    font-size: 2rem;
    line-height: 1.08;
    letter-spacing: -0.04em;
  }

  .visual-line {
    width: 70px;
    height: 3px;
    margin-top: 28px;
    background: #4de4ff;
  }

  .visual-caption {
    display: inline-block;
    margin-top: 22px;
    color: #4de4ff;
    font-size: 0.65rem;
    font-weight: 800;
    letter-spacing: 0.12em;
  }

  .floating-tag {
    position: absolute;
    z-index: 3;
    padding: 13px 18px;
    border: 1px solid rgba(77, 228, 255, 0.45);
    border-radius: 10px;
    background: #0b2538;
    color: #4de4ff;
    font-size: 0.7rem;
    font-weight: 800;
    letter-spacing: 0.1em;
    white-space: nowrap;
  }

  .tag-one {
    top: 80px;
    left: 0;
  }

  .tag-two {
    right: -5px;
    top: 145px;
  }

  .tag-three {
    bottom: 75px;
    left: 30px;
  }

  /* Benefits */

  .benefits-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 16px;
  }

  .benefit-card {
    min-height: 205px;
    padding: 28px 25px;
    border: 1px solid rgba(77, 228, 255, 0.2);
    border-radius: 22px;
    background: rgba(7, 26, 43, 0.65);
    transition:
      border-color 0.25s ease,
      background 0.25s ease,
      transform 0.25s ease;
  }

  .benefit-card:hover {
    border-color: rgba(77, 228, 255, 0.8);
    background: rgba(15, 49, 68, 0.7);
    transform: translateY(-4px);
  }

  .card-number {
    display: block;
    margin-bottom: 30px;
    color: #4de4ff;
    font-size: 0.8rem;
    font-weight: 800;
  }

  .benefit-card h3 {
    margin: 0 0 15px;
    color: #ffffff;
    font-size: 1.2rem;
    line-height: 1.25;
  }

  .benefit-card p {
    margin: 0;
    color: #a8bac9;
    font-size: 0.95rem;
    line-height: 1.65;
  }

  .closing-line {
    margin-top: 70px;
    text-align: center;
    color: #4de4ff;
    font-size: 1.2rem;
    font-weight: 800;
  }

  /* Responsive design */

  @media (max-width: 1100px) {
    .content-grid {
      grid-template-columns: 1fr;
      gap: 55px;
    }

    .experience-visual {
      min-height: 470px;
    }

    .benefits-grid {
      max-width: 850px;
      width: 100%;
      margin: 0 auto;
    }
  }

  @media (max-width: 650px) {
    .why-join {
      padding: 80px 5% 75px;
    }

    .section-heading {
      margin-bottom: 50px;
    }

    .section-heading h2 {
      font-size: 2.65rem;
    }

    .section-heading p {
      font-size: 1rem;
    }

    .experience-visual {
      min-height: 390px;
      transform: scale(0.85);
      margin: -25px 0;
    }

    .benefits-grid {
      grid-template-columns: 1fr;
    }

    .benefit-card {
      min-height: auto;
    }

    .closing-line {
      margin-top: 50px;
      font-size: 1rem;
      line-height: 1.6;
    }
  }
</style>