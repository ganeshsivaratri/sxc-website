<section class="why-join" id="why-join">
  <div class="section-container">

    <!-- =========================================================
         SECTION INTRO
    ========================================================== -->

    <div class="section-intro">

      <div class="intro-copy">
        <div class="eyebrow-wrap">
          <span class="eyebrow-line"></span>
          <span class="eyebrow">WHY JOIN SXC</span>
        </div>

        <h2>
          Build Experience
          <span>That Goes Beyond</span>
          <strong>the Classroom.</strong>
        </h2>
      </div>

      <div class="intro-description">
        <div class="description-index">01</div>

        <p>
          Turn your knowledge into practical experience by working on projects,
          collaborating with teams, and contributing to product development.
        </p>

        <div class="description-line"></div>
      </div>

    </div>


    <!-- =========================================================
         EXPERIENCE STAGE
    ========================================================== -->

    <div class="experience-stage">

      <!-- Background decorative grid -->
      <div class="stage-grid"></div>

      <!-- Animated glow -->
      <div class="stage-glow"></div>

      <!-- =====================================================
           LEFT VISUAL
      ====================================================== -->

      <div class="experience-visual">

        <div class="visual-top-label">
          <span class="status-dot"></span>
          SXC EXPERIENCE SYSTEM
        </div>

        <!-- Orbit system -->
        <div class="orbit orbit-one"></div>
        <div class="orbit orbit-two"></div>
        <div class="orbit orbit-three"></div>

        <div class="orbit-dot dot-one"></div>
        <div class="orbit-dot dot-two"></div>
        <div class="orbit-dot dot-three"></div>

        <!-- Main experience card -->
        <div class="visual-card">

          <div class="card-top">
            <span>SXC</span>
            <span class="card-status">ACTIVE</span>
          </div>

          <div class="card-main">

            <span class="card-small-label">
              YOUR NEXT LEVEL
            </span>

            <h3>
              BUILD
              <br />
              YOUR
              <br />
              <span>EXPERIENCE.</span>
            </h3>

            <div class="visual-line"></div>

            <p>
              Learn by contributing to work that actually moves forward.
            </p>

          </div>

          <div class="card-bottom">
            <span>REAL PROJECTS</span>
            <span>01 / 06</span>
          </div>

        </div>


        <!-- Floating tags -->

        <div class="floating-tag tag-one">
          <span>01</span>
          REAL PROJECTS
        </div>

        <div class="floating-tag tag-two">
          <span>02</span>
          TEAMWORK
        </div>

        <div class="floating-tag tag-three">
          <span>03</span>
          REAL EXPERIENCE
        </div>

        <div class="floating-tag tag-four">
          <span>+</span>
          PORTFOLIO
        </div>


        <!-- Bottom visual statement -->

        <div class="visual-footer">
          <span class="footer-dot"></span>

          <span>
            KNOWLEDGE
            <b>→</b>
            CONTRIBUTION
            <b>→</b>
            EXPERIENCE
          </span>
        </div>

      </div>


      <!-- =====================================================
           RIGHT BENEFITS
      ====================================================== -->

      <div class="benefits-area">

        <div class="benefits-heading">
          <span>WHAT YOU BUILD</span>

          <p>
            Your SXC journey is designed around practical contribution,
            collaboration, and visible growth.
          </p>
        </div>


        <div class="benefits-grid">

          <!-- 01 -->
          <article class="benefit-card">

            <div class="card-number">
              01
            </div>

            <div class="benefit-content">
              <h3>
                Real Project
                Experience
              </h3>

              <p>
                Work on practical projects and understand how real products
                are developed.
              </p>
            </div>

            <div class="card-arrow">↗</div>

          </article>


          <!-- 02 -->
          <article class="benefit-card">

            <div class="card-number">
              02
            </div>

            <div class="benefit-content">
              <h3>
                Collaborative
                Learning
              </h3>

              <p>
                Learn from teammates while sharing ideas, skills, and
                responsibilities.
              </p>
            </div>

            <div class="card-arrow">↗</div>

          </article>


          <!-- 03 -->
          <article class="benefit-card">

            <div class="card-number">
              03
            </div>

            <div class="benefit-content">
              <h3>
                Portfolio
                Building
              </h3>

              <p>
                Create meaningful work that demonstrates your skills and
                contributions.
              </p>
            </div>

            <div class="card-arrow">↗</div>

          </article>


          <!-- 04 -->
          <article class="benefit-card">

            <div class="card-number">
              04
            </div>

            <div class="benefit-content">
              <h3>
                Professional
                Growth
              </h3>

              <p>
                Strengthen your communication, teamwork, and problem-solving
                abilities.
              </p>
            </div>

            <div class="card-arrow">↗</div>

          </article>


          <!-- 05 -->
          <article class="benefit-card featured-card">

            <div class="card-number">
              05
            </div>

            <div class="benefit-content">
              <h3>
                SXC
                Certificate
              </h3>

              <p>
                Receive an SXC certificate upon completing your approved
                project journey.
              </p>
            </div>

            <div class="card-arrow">↗</div>

          </article>


          <!-- 06 -->
          <article class="benefit-card">

            <div class="card-number">
              06
            </div>

            <div class="benefit-content">
              <h3>
                Startup
                Exposure
              </h3>

              <p>
                Experience a startup-oriented environment without a promise
                of employment or salary.
              </p>
            </div>

            <div class="card-arrow">↗</div>

          </article>

        </div>

      </div>

    </div>


    <!-- =========================================================
         CLOSING STATEMENT
    ========================================================== -->

    <div class="closing-section">

      <div class="closing-number">
        06
      </div>

      <div class="closing-line"></div>

      <p>
        The goal is not simply to
        <span>learn more.</span>
        <br />
        It is to
        <strong>build something that proves it.</strong>
      </p>

      <div class="closing-mark">
        SXC
      </div>

    </div>

  </div>
</section>


<style>

  /* ============================================================
     BASE SECTION
  ============================================================ */

  .why-join {
    position: relative;
    width: 100%;
    padding: 140px 6% 120px;
    background: #ffffff;
    color: #071a2b;
    overflow: hidden;
  }

  .section-container {
    width: 100%;
    max-width: 1420px;
    margin: 0 auto;
  }


  /* ============================================================
     INTRO
  ============================================================ */

  .section-intro {
    display: grid;
    grid-template-columns: minmax(0, 1.25fr) minmax(300px, 0.75fr);
    gap: 80px;
    align-items: end;
    margin-bottom: 80px;
  }

  .eyebrow-wrap {
    display: flex;
    align-items: center;
    gap: 14px;
    margin-bottom: 28px;
  }

  .eyebrow-line {
    width: 42px;
    height: 2px;
    background: #0b8fa8;
  }

  .eyebrow {
    color: #0b8fa8;
    font-size: 0.72rem;
    font-weight: 900;
    letter-spacing: 0.22em;
  }

  .section-intro h2 {
    max-width: 950px;
    margin: 0;
    font-size: clamp(3.4rem, 6vw, 6.8rem);
    font-weight: 800;
    line-height: 0.96;
    letter-spacing: -0.065em;
  }

  .section-intro h2 span {
    display: block;
    color: #0b8fa8;
  }

  .section-intro h2 strong {
    display: block;
    font-weight: 800;
    color: #071a2b;
  }


  /* ============================================================
     INTRO DESCRIPTION
  ============================================================ */

  .intro-description {
    position: relative;
    padding: 0 0 10px 30px;
  }

  .description-index {
    position: absolute;
    top: -35px;
    left: 0;
    color: rgba(7, 26, 43, 0.16);
    font-size: 5rem;
    font-weight: 900;
    line-height: 1;
    letter-spacing: -0.08em;
  }

  .intro-description p {
    position: relative;
    z-index: 2;
    max-width: 430px;
    margin: 0;
    color: #536473;
    font-size: 1.05rem;
    line-height: 1.8;
  }

  .description-line {
    width: 100%;
    height: 1px;
    margin-top: 28px;
    background: linear-gradient(
      90deg,
      #071a2b,
      rgba(7, 26, 43, 0.08)
    );
  }


  /* ============================================================
     EXPERIENCE STAGE
  ============================================================ */

  .experience-stage {
    position: relative;
    display: grid;
    grid-template-columns: 0.9fr 1.1fr;
    min-height: 720px;
    border: 1px solid rgba(7, 26, 43, 0.1);
    background: #f6f9fb;
    overflow: hidden;
  }

  .stage-grid {
    position: absolute;
    inset: 0;
    opacity: 0.5;
    background-image:
      linear-gradient(
        rgba(7, 26, 43, 0.045) 1px,
        transparent 1px
      ),
      linear-gradient(
        90deg,
        rgba(7, 26, 43, 0.045) 1px,
        transparent 1px
      );
    background-size: 55px 55px;
    pointer-events: none;
  }

  .stage-glow {
    position: absolute;
    width: 550px;
    height: 550px;
    left: 8%;
    top: 15%;
    border-radius: 50%;
    background: radial-gradient(
      circle,
      rgba(77, 228, 255, 0.13),
      transparent 68%
    );
    filter: blur(10px);
    animation: glowPulse 5s ease-in-out infinite;
    pointer-events: none;
  }


  /* ============================================================
     EXPERIENCE VISUAL
  ============================================================ */

  .experience-visual {
    position: relative;
    min-height: 720px;
    display: flex;
    align-items: center;
    justify-content: center;
    border-right: 1px solid rgba(7, 26, 43, 0.1);
    overflow: hidden;
  }

  .visual-top-label {
    position: absolute;
    top: 32px;
    left: 35px;
    display: flex;
    align-items: center;
    gap: 9px;
    color: #60717e;
    font-size: 0.65rem;
    font-weight: 900;
    letter-spacing: 0.14em;
  }

  .status-dot,
  .footer-dot {
    width: 7px;
    height: 7px;
    border-radius: 50%;
    background: #12b886;
    box-shadow: 0 0 0 5px rgba(18, 184, 134, 0.1);
    animation: statusPulse 2s ease-in-out infinite;
  }


  /* ============================================================
     ORBITS
  ============================================================ */

  .orbit {
    position: absolute;
    border: 1px solid rgba(11, 143, 168, 0.18);
    border-radius: 50%;
    pointer-events: none;
  }

  .orbit-one {
    width: 470px;
    height: 300px;
    transform: rotate(-25deg);
    animation: orbitFloat 7s ease-in-out infinite;
  }

  .orbit-two {
    width: 540px;
    height: 360px;
    transform: rotate(28deg);
    animation: orbitFloatReverse 9s ease-in-out infinite;
  }

  .orbit-three {
    width: 400px;
    height: 400px;
    animation: orbitSpin 24s linear infinite;
  }

  .orbit-dot {
    position: absolute;
    width: 9px;
    height: 9px;
    border-radius: 50%;
    background: #4de4ff;
    box-shadow: 0 0 18px rgba(77, 228, 255, 0.8);
  }

  .dot-one {
    top: 27%;
    right: 18%;
    animation: dotFloat 4s ease-in-out infinite;
  }

  .dot-two {
    bottom: 25%;
    left: 19%;
    animation: dotFloat 5s ease-in-out infinite reverse;
  }

  .dot-three {
    top: 21%;
    left: 28%;
    width: 6px;
    height: 6px;
    animation: dotFloat 3.5s ease-in-out infinite;
  }


  /* ============================================================
     MAIN VISUAL CARD
  ============================================================ */

  .visual-card {
    position: relative;
    z-index: 5;
    width: 330px;
    min-height: 430px;
    padding: 28px;
    border: 1px solid rgba(7, 26, 43, 0.25);
    background: #071a2b;
    color: #ffffff;
    box-shadow:
      0 35px 80px rgba(7, 26, 43, 0.18),
      0 0 0 10px rgba(255, 255, 255, 0.6);
    animation: cardEntrance 1s cubic-bezier(0.22, 1, 0.36, 1) both;
    transition:
      transform 0.5s ease,
      box-shadow 0.5s ease;
  }

  .visual-card:hover {
    transform: translateY(-8px) rotate(-1deg);
    box-shadow:
      0 45px 100px rgba(7, 26, 43, 0.24),
      0 0 0 10px rgba(255, 255, 255, 0.75);
  }

  .card-top,
  .card-bottom {
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  .card-top {
    padding-bottom: 18px;
    border-bottom: 1px solid rgba(255, 255, 255, 0.14);
    color: #4de4ff;
    font-size: 0.7rem;
    font-weight: 900;
    letter-spacing: 0.16em;
  }

  .card-status {
    color: #8dabb9;
    font-size: 0.55rem;
    letter-spacing: 0.14em;
  }

  .card-main {
    padding: 48px 0 55px;
  }

  .card-small-label {
    color: #91a9b6;
    font-size: 0.6rem;
    font-weight: 800;
    letter-spacing: 0.18em;
  }

  .visual-card h3 {
    margin: 22px 0 0;
    font-size: 3rem;
    font-weight: 800;
    line-height: 0.92;
    letter-spacing: -0.07em;
  }

  .visual-card h3 span {
    color: #4de4ff;
  }

  .visual-line {
    width: 65px;
    height: 3px;
    margin-top: 30px;
    background: #4de4ff;
  }

  .card-main p {
    max-width: 230px;
    margin: 24px 0 0;
    color: #a8bac9;
    font-size: 0.78rem;
    line-height: 1.65;
  }

  .card-bottom {
    padding-top: 17px;
    border-top: 1px solid rgba(255, 255, 255, 0.14);
    color: #8da4b1;
    font-size: 0.55rem;
    font-weight: 800;
    letter-spacing: 0.14em;
  }


  /* ============================================================
     FLOATING TAGS
  ============================================================ */

  .floating-tag {
    position: absolute;
    z-index: 8;
    display: flex;
    align-items: center;
    gap: 9px;
    padding: 11px 14px;
    border: 1px solid rgba(7, 26, 43, 0.12);
    background: rgba(255, 255, 255, 0.9);
    color: #071a2b;
    box-shadow: 0 15px 35px rgba(7, 26, 43, 0.08);
    backdrop-filter: blur(10px);
    font-size: 0.58rem;
    font-weight: 900;
    letter-spacing: 0.1em;
    white-space: nowrap;
  }

  .floating-tag span {
    color: #0b8fa8;
    font-weight: 900;
  }

  .tag-one {
    top: 17%;
    left: 4%;
    animation: tagFloatOne 5s ease-in-out infinite;
  }

  .tag-two {
    top: 27%;
    right: 2%;
    animation: tagFloatTwo 6s ease-in-out infinite;
  }

  .tag-three {
    bottom: 24%;
    left: 4%;
    animation: tagFloatThree 5.5s ease-in-out infinite;
  }

  .tag-four {
    right: 6%;
    bottom: 15%;
    animation: tagFloatTwo 5s ease-in-out infinite reverse;
  }


  /* ============================================================
     VISUAL FOOTER
  ============================================================ */

  .visual-footer {
    position: absolute;
    bottom: 30px;
    left: 35px;
    right: 35px;
    display: flex;
    align-items: center;
    gap: 12px;
    color: #748792;
    font-size: 0.55rem;
    font-weight: 900;
    letter-spacing: 0.1em;
  }

  .visual-footer b {
    margin: 0 5px;
    color: #0b8fa8;
  }


  /* ============================================================
     BENEFITS AREA
  ============================================================ */

  .benefits-area {
    position: relative;
    z-index: 2;
    padding: 58px 55px;
  }

  .benefits-heading {
    display: flex;
    align-items: flex-end;
    justify-content: space-between;
    gap: 40px;
    margin-bottom: 35px;
  }

  .benefits-heading > span {
    color: #071a2b;
    font-size: 0.68rem;
    font-weight: 900;
    letter-spacing: 0.18em;
  }

  .benefits-heading p {
    max-width: 350px;
    margin: 0;
    color: #71818d;
    font-size: 0.82rem;
    line-height: 1.65;
  }


  /* ============================================================
     BENEFITS GRID
  ============================================================ */

  .benefits-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 12px;
  }

  .benefit-card {
    position: relative;
    min-height: 190px;
    padding: 25px;
    border: 1px solid rgba(7, 26, 43, 0.11);
    background: rgba(255, 255, 255, 0.82);
    overflow: hidden;
    transition:
      transform 0.4s cubic-bezier(0.22, 1, 0.36, 1),
      border-color 0.35s ease,
      box-shadow 0.35s ease,
      background 0.35s ease;
    animation: benefitEntrance 0.8s cubic-bezier(0.22, 1, 0.36, 1) both;
  }

  .benefit-card:nth-child(1) {
    animation-delay: 0.08s;
  }

  .benefit-card:nth-child(2) {
    animation-delay: 0.16s;
  }

  .benefit-card:nth-child(3) {
    animation-delay: 0.24s;
  }

  .benefit-card:nth-child(4) {
    animation-delay: 0.32s;
  }

  .benefit-card:nth-child(5) {
    animation-delay: 0.4s;
  }

  .benefit-card:nth-child(6) {
    animation-delay: 0.48s;
  }

  .benefit-card::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 2px;
    background: linear-gradient(
      90deg,
      #071a2b,
      #4de4ff,
      transparent
    );
    transform: scaleX(0);
    transform-origin: left;
    transition: transform 0.45s ease;
  }

  .benefit-card::after {
    content: "";
    position: absolute;
    width: 130px;
    height: 130px;
    right: -70px;
    bottom: -70px;
    border-radius: 50%;
    background: rgba(77, 228, 255, 0.12);
    transition:
      transform 0.5s ease,
      opacity 0.5s ease;
  }

  .benefit-card:hover {
    transform: translateY(-8px);
    border-color: rgba(11, 143, 168, 0.35);
    background: #ffffff;
    box-shadow: 0 25px 55px rgba(7, 26, 43, 0.09);
  }

  .benefit-card:hover::before {
    transform: scaleX(1);
  }

  .benefit-card:hover::after {
    transform: scale(2);
    opacity: 0.5;
  }

  .card-number {
    position: relative;
    z-index: 2;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 31px;
    height: 31px;
    border: 1px solid rgba(11, 143, 168, 0.25);
    border-radius: 50%;
    color: #0b8fa8;
    font-size: 0.58rem;
    font-weight: 900;
  }

  .benefit-content {
    position: relative;
    z-index: 2;
    padding-right: 20px;
  }

  .benefit-card h3 {
    margin: 24px 0 11px;
    color: #071a2b;
    font-size: 1.15rem;
    font-weight: 800;
    line-height: 1.15;
    letter-spacing: -0.025em;
  }

  .benefit-card p {
    margin: 0;
    color: #71818d;
    font-size: 0.78rem;
    line-height: 1.65;
  }

  .card-arrow {
    position: absolute;
    right: 22px;
    top: 24px;
    color: rgba(7, 26, 43, 0.3);
    font-size: 1rem;
    transition:
      transform 0.35s ease,
      color 0.35s ease;
  }

  .benefit-card:hover .card-arrow {
    color: #0b8fa8;
    transform: translate(4px, -4px);
  }

  .featured-card {
    background: #071a2b;
    border-color: #071a2b;
  }

  .featured-card .card-number {
    border-color: rgba(77, 228, 255, 0.3);
    color: #4de4ff;
  }

  .featured-card h3 {
    color: #ffffff;
  }

  .featured-card p {
    color: #9eb1bd;
  }

  .featured-card .card-arrow {
    color: #4de4ff;
  }

  .featured-card:hover {
    background: #0a2639;
    border-color: #0a2639;
  }


  /* ============================================================
     CLOSING
  ============================================================ */

  .closing-section {
    display: grid;
    grid-template-columns: auto 1fr auto;
    align-items: center;
    gap: 25px;
    margin-top: 70px;
  }

  .closing-number {
    color: rgba(7, 26, 43, 0.2);
    font-size: 0.65rem;
    font-weight: 900;
    letter-spacing: 0.12em;
  }

  .closing-line {
    height: 1px;
    background: linear-gradient(
      90deg,
      rgba(7, 26, 43, 0.2),
      rgba(7, 26, 43, 0)
    );
  }

  .closing-section p {
    grid-column: 1 / -1;
    max-width: 900px;
    margin: 0 auto;
    text-align: center;
    color: #536473;
    font-size: clamp(1.4rem, 2.5vw, 2.2rem);
    line-height: 1.35;
    letter-spacing: -0.035em;
  }

  .closing-section p span {
    color: #0b8fa8;
  }

  .closing-section p strong {
    color: #071a2b;
  }

  .closing-mark {
    color: rgba(7, 26, 43, 0.2);
    font-size: 0.65rem;
    font-weight: 900;
    letter-spacing: 0.15em;
  }


  /* ============================================================
     ANIMATIONS
  ============================================================ */

  @keyframes cardEntrance {
    from {
      opacity: 0;
      transform: translateY(35px) rotate(-4deg) scale(0.96);
    }

    to {
      opacity: 1;
      transform: translateY(0) rotate(0) scale(1);
    }
  }

  @keyframes benefitEntrance {
    from {
      opacity: 0;
      transform: translateY(25px);
    }

    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes orbitFloat {
    0%,
    100% {
      transform: rotate(-25deg) translateY(0);
    }

    50% {
      transform: rotate(-21deg) translateY(-14px);
    }
  }

  @keyframes orbitFloatReverse {
    0%,
    100% {
      transform: rotate(28deg) translateY(0);
    }

    50% {
      transform: rotate(32deg) translateY(12px);
    }
  }

  @keyframes orbitSpin {
    from {
      transform: rotate(0deg);
    }

    to {
      transform: rotate(360deg);
    }
  }

  @keyframes dotFloat {
    0%,
    100% {
      transform: translate(0, 0);
    }

    50% {
      transform: translate(8px, -12px);
    }
  }

  @keyframes tagFloatOne {
    0%,
    100% {
      transform: translate(0, 0);
    }

    50% {
      transform: translate(5px, -8px);
    }
  }

  @keyframes tagFloatTwo {
    0%,
    100% {
      transform: translate(0, 0);
    }

    50% {
      transform: translate(-7px, 8px);
    }
  }

  @keyframes tagFloatThree {
    0%,
    100% {
      transform: translate(0, 0);
    }

    50% {
      transform: translate(7px, -5px);
    }
  }

  @keyframes glowPulse {
    0%,
    100% {
      opacity: 0.55;
      transform: scale(1);
    }

    50% {
      opacity: 1;
      transform: scale(1.08);
    }
  }

  @keyframes statusPulse {
    0%,
    100% {
      box-shadow: 0 0 0 4px rgba(18, 184, 134, 0.08);
    }

    50% {
      box-shadow: 0 0 0 8px rgba(18, 184, 134, 0.02);
    }
  }


  /* ============================================================
     TABLET
  ============================================================ */

  @media (max-width: 1100px) {

    .why-join {
      padding: 110px 5% 100px;
    }

    .section-intro {
      grid-template-columns: 1fr;
      gap: 35px;
    }

    .intro-description {
      max-width: 650px;
      padding-left: 0;
    }

    .description-index {
      display: none;
    }

    .experience-stage {
      grid-template-columns: 1fr;
    }

    .experience-visual {
      min-height: 650px;
      border-right: 0;
      border-bottom: 1px solid rgba(7, 26, 43, 0.1);
    }

    .benefits-area {
      padding: 55px;
    }

  }


  /* ============================================================
     MOBILE
  ============================================================ */

  @media (max-width: 700px) {

    .why-join {
      padding: 85px 5% 80px;
    }

    .section-intro {
      gap: 30px;
      margin-bottom: 55px;
    }

    .eyebrow-wrap {
      margin-bottom: 22px;
    }

    .section-intro h2 {
      font-size: clamp(2.9rem, 13vw, 4.2rem);
      line-height: 0.98;
    }

    .intro-description p {
      font-size: 0.95rem;
      line-height: 1.7;
    }


    /* Stage */

    .experience-stage {
      min-height: auto;
    }

    .experience-visual {
      min-height: 550px;
    }

    .visual-top-label {
      top: 22px;
      left: 20px;
      font-size: 0.55rem;
    }

    .visual-card {
      width: 255px;
      min-height: 350px;
      padding: 22px;
    }

    .card-main {
      padding: 38px 0 40px;
    }

    .visual-card h3 {
      font-size: 2.35rem;
    }

    .orbit-one {
      width: 350px;
      height: 230px;
    }

    .orbit-two {
      width: 400px;
      height: 280px;
    }

    .orbit-three {
      width: 300px;
      height: 300px;
    }

    .tag-one {
      top: 17%;
      left: 3%;
    }

    .tag-two {
      top: 29%;
      right: 2%;
    }

    .tag-three {
      bottom: 21%;
      left: 3%;
    }

    .tag-four {
      right: 3%;
      bottom: 13%;
    }

    .floating-tag {
      padding: 9px 10px;
      font-size: 0.48rem;
    }

    .visual-footer {
      bottom: 20px;
      left: 20px;
      right: 20px;
      font-size: 0.45rem;
      line-height: 1.6;
    }


    /* Benefits */

    .benefits-area {
      padding: 45px 20px;
    }

    .benefits-heading {
      display: block;
      margin-bottom: 28px;
    }

    .benefits-heading p {
      margin-top: 15px;
      font-size: 0.78rem;
    }

    .benefits-grid {
      grid-template-columns: 1fr;
      gap: 10px;
    }

    .benefit-card {
      min-height: 175px;
    }

    .benefit-card h3 {
      font-size: 1.1rem;
    }

    .benefit-card p {
      font-size: 0.76rem;
    }


    /* Closing */

    .closing-section {
      grid-template-columns: 1fr;
      gap: 15px;
      margin-top: 55px;
      text-align: center;
    }

    .closing-line {
      display: none;
    }

    .closing-number,
    .closing-mark {
      display: none;
    }

    .closing-section p {
      font-size: 1.35rem;
    }

  }


  /* ============================================================
     REDUCED MOTION
  ============================================================ */

  @media (prefers-reduced-motion: reduce) {

    .visual-card,
    .benefit-card,
    .orbit,
    .orbit-dot,
    .floating-tag,
    .stage-glow,
    .status-dot,
    .footer-dot {
      animation: none !important;
    }

    .visual-card,
    .benefit-card {
      transition: none !important;
    }

  }

</style>