<section class="hero">
  <div class="hero-container">
    <!-- Left: Main message -->
    <div class="hero-content">
      <p class="eyebrow">STUDENTS • STARTUPS • REAL IMPACT</p>

      <h1>
        Work on <span>Startups.</span><br />
        Build Real Projects.<br />
        Gain Real Experience.
      </h1>

      <p class="hero-description">
        Join a student-powered environment where teams turn real-world
        problems into practical products, explore startup opportunities,
        and gain experience through execution.
      </p>

      <div class="hero-actions">
        <a href="/join" class="primary-button">
          Join SXC <span>↗</span>
        </a>

        <a href="/projects" class="secondary-button">
          Explore Projects
        </a>
      </div>

      <div class="hero-stats">
        <div class="stat">
          <strong>45</strong>
          <span>Days</span>
        </div>

        <div class="stat">
          <strong>1</strong>
          <span>Team</span>
        </div>

        <div class="stat">
          <strong>1</strong>
          <span>Project</span>
        </div>
      </div>
    </div>

    <!-- Right: Product workspace preview -->
    <div class="workspace-wrapper">
      <div class="workspace-glow"></div>

      <div class="workspace">
        <!-- Workspace header -->
        <div class="workspace-header">
          <div class="workspace-brand">
            <div class="brand-mark">S</div>

            <div>
              <strong>SXC Workspace</strong>
              <span>Build Mode</span>
            </div>
          </div>

          <div class="workspace-status">
            <span></span>
            Live
          </div>
        </div>

        <!-- Workspace navigation -->
        <div class="workspace-body">
          <aside class="workspace-sidebar">
            <div class="sidebar-item active">
              <span>⌂</span>
              Overview
            </div>

            <div class="sidebar-item">
              <span>▦</span>
              Projects
            </div>

            <div class="sidebar-item">
              <span>♧</span>
              Team
            </div>

            <div class="sidebar-item">
              <span>✓</span>
              Tasks
            </div>

            <div class="sidebar-item">
              <span>◈</span>
              Community
            </div>
          </aside>

          <!-- Main dashboard -->
          <div class="dashboard">
            <div class="dashboard-heading">
              <div>
                <p>YOUR BUILDING JOURNEY</p>
                <h2>Build Something Real.</h2>
              </div>

              <div class="avatar">A</div>
            </div>

            <p class="dashboard-description">
              Ideas become products through teamwork.
            </p>

            <!-- Progress card -->
            <div class="progress-card">
              <div class="card-topline">
                <span>Project Progress</span>
                <strong>68%</strong>
              </div>

              <div class="progress-track">
                <div class="progress-fill"></div>
              </div>

              <div class="progress-meta">
                <span>Discovery completed</span>
                <span>Build in progress</span>
              </div>
            </div>

            <!-- Feature cards -->
            <div class="feature-grid">
              <div class="feature-card">
                <div class="feature-icon cyan">✦</div>

                <div>
                  <strong>Startups</strong>
                  <p>Explore opportunities</p>
                </div>

                <span class="feature-arrow">↗</span>
              </div>

              <div class="feature-card">
                <div class="feature-icon purple">▦</div>

                <div>
                  <strong>Projects</strong>
                  <p>Build useful products</p>
                </div>

                <span class="feature-arrow">↗</span>
              </div>

              <div class="feature-card">
                <div class="feature-icon green">♧</div>

                <div>
                  <strong>Teams</strong>
                  <p>Collaborate and grow</p>
                </div>

                <span class="feature-arrow">↗</span>
              </div>

              <div class="feature-card">
                <div class="feature-icon orange">◈</div>

                <div>
                  <strong>Experience</strong>
                  <p>Prove your contribution</p>
                </div>

                <span class="feature-arrow">↗</span>
              </div>
            </div>

            <!-- Bottom workspace status -->
            <div class="workspace-footer">
              <div>
                <span class="footer-dot"></span>
                Startup Lab
              </div>

              <span>Team connected</span>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    position: relative;
    min-height: 100vh;
    overflow: hidden;
    padding: 170px 6% 90px;
    background:
      radial-gradient(
        circle at 78% 48%,
        rgba(24, 213, 232, 0.08),
        transparent 32%
      ),
      #071a2b;
    color: #ffffff;
  }

  .hero-container {
    position: relative;
    z-index: 1;
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(480px, 1fr);
    align-items: center;
    gap: 70px;
    max-width: 1500px;
    min-height: 680px;
    margin: 0 auto;
  }

  .hero-content {
    max-width: 700px;
  }

  .eyebrow {
    margin: 0 0 28px;
    color: #18d5e8;
    font-size: 0.85rem;
    font-weight: 800;
    letter-spacing: 0.18em;
  }

  h1 {
    margin: 0;
    font-size: clamp(3.2rem, 5vw, 5.5rem);
    font-weight: 800;
    line-height: 1.04;
    letter-spacing: -0.055em;
  }

  h1 span {
    color: #18d5e8;
  }

  .hero-description {
    max-width: 620px;
    margin: 35px 0 0;
    color: #a9bfce;
    font-size: 1.1rem;
    line-height: 1.8;
  }

  .hero-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 16px;
    margin-top: 35px;
  }

  .primary-button,
  .secondary-button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 14px;
    min-height: 58px;
    padding: 0 27px;
    border-radius: 13px;
    font-size: 1rem;
    font-weight: 750;
    text-decoration: none;
    transition:
      transform 0.25s ease,
      box-shadow 0.25s ease,
      background 0.25s ease;
  }

  .primary-button {
    background: linear-gradient(135deg, #18d5e8, #2f80ed);
    color: #061522;
    box-shadow: 0 12px 35px rgba(24, 213, 232, 0.15);
  }

  .secondary-button {
    border: 1px solid rgba(24, 213, 232, 0.7);
    color: #ffffff;
  }

  .primary-button:hover,
  .secondary-button:hover {
    transform: translateY(-3px);
  }

  .primary-button:hover {
    box-shadow: 0 16px 40px rgba(24, 213, 232, 0.25);
  }

  .secondary-button:hover {
    background: rgba(24, 213, 232, 0.08);
  }

  .hero-stats {
    display: flex;
    gap: 55px;
    margin-top: 60px;
  }

  .stat {
    display: flex;
    flex-direction: column;
    gap: 5px;
  }

  .stat strong {
    color: #18d5e8;
    font-size: 3rem;
    line-height: 1;
    letter-spacing: -0.04em;
  }

  .stat span {
    color: #a9bfce;
    font-size: 0.95rem;
  }

  .workspace-wrapper {
    position: relative;
    width: 100%;
  }

  .workspace-glow {
    position: absolute;
    inset: 12% -8% -10%;
    background: rgba(24, 213, 232, 0.12);
    filter: blur(90px);
    pointer-events: none;
  }

  .workspace {
    position: relative;
    overflow: hidden;
    border: 1px solid rgba(116, 194, 218, 0.24);
    border-radius: 24px;
    background: rgba(9, 35, 55, 0.92);
    box-shadow:
      0 35px 100px rgba(0, 0, 0, 0.35),
      inset 0 1px 0 rgba(255, 255, 255, 0.05);
    backdrop-filter: blur(20px);
  }

  .workspace-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 24px 26px;
    border-bottom: 1px solid rgba(167, 211, 224, 0.1);
  }

  .workspace-brand {
    display: flex;
    align-items: center;
    gap: 12px;
  }

  .brand-mark {
    display: grid;
    width: 40px;
    height: 40px;
    place-items: center;
    border: 1px solid rgba(24, 213, 232, 0.5);
    border-radius: 12px;
    background: rgba(24, 213, 232, 0.1);
    color: #18d5e8;
    font-size: 1.2rem;
    font-weight: 800;
  }

  .workspace-brand strong,
  .workspace-brand span {
    display: block;
  }

  .workspace-brand strong {
    font-size: 0.95rem;
  }

  .workspace-brand span {
    margin-top: 4px;
    color: #7596a9;
    font-size: 0.75rem;
  }

  .workspace-status {
    display: flex;
    align-items: center;
    gap: 7px;
    color: #91cdb4;
    font-size: 0.75rem;
  }

  .workspace-status span,
  .footer-dot {
    width: 7px;
    height: 7px;
    border-radius: 50%;
    background: #27d17f;
    box-shadow: 0 0 12px rgba(39, 209, 127, 0.7);
  }

  .workspace-body {
    display: grid;
    grid-template-columns: 125px minmax(0, 1fr);
    min-height: 440px;
  }

  .workspace-sidebar {
    padding: 24px 12px;
    border-right: 1px solid rgba(167, 211, 224, 0.1);
  }

  .sidebar-item {
    display: flex;
    align-items: center;
    gap: 10px;
    margin-bottom: 9px;
    padding: 11px 10px;
    border-radius: 9px;
    color: #7898aa;
    font-size: 0.7rem;
    white-space: nowrap;
  }

  .sidebar-item span {
    font-size: 0.9rem;
  }

  .sidebar-item.active {
    background: rgba(24, 213, 232, 0.12);
    color: #18d5e8;
  }

  .dashboard {
    min-width: 0;
    padding: 28px;
  }

  .dashboard-heading {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: 15px;
  }

  .dashboard-heading p {
    margin: 0 0 9px;
    color: #18d5e8;
    font-size: 0.62rem;
    font-weight: 800;
    letter-spacing: 0.13em;
  }

  .dashboard-heading h2 {
    margin: 0;
    font-size: 1.25rem;
    letter-spacing: -0.035em;
  }

  .avatar {
    display: grid;
    width: 34px;
    height: 34px;
    place-items: center;
    border-radius: 50%;
    background: linear-gradient(135deg, #7c4dff, #2f80ed);
    font-size: 0.8rem;
    font-weight: 800;
  }

  .dashboard-description {
    margin: 12px 0 25px;
    color: #7898aa;
    font-size: 0.78rem;
  }

  .progress-card {
    padding: 18px;
    border: 1px solid rgba(167, 211, 224, 0.1);
    border-radius: 14px;
    background: rgba(4, 22, 37, 0.45);
  }

  .card-topline,
  .progress-meta {
    display: flex;
    justify-content: space-between;
    gap: 15px;
  }

  .card-topline {
    color: #9eb7c5;
    font-size: 0.72rem;
  }

  .card-topline strong {
    color: #18d5e8;
  }

  .progress-track {
    height: 7px;
    margin: 17px 0 12px;
    overflow: hidden;
    border-radius: 20px;
    background: #18384c;
  }

  .progress-fill {
    width: 68%;
    height: 100%;
    border-radius: inherit;
    background: linear-gradient(90deg, #18d5e8, #2f80ed);
  }

  .progress-meta {
    color: #617f91;
    font-size: 0.62rem;
  }

  .feature-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 12px;
    margin-top: 15px;
  }

  .feature-card {
    display: flex;
    align-items: center;
    gap: 10px;
    min-width: 0;
    padding: 15px 12px;
    border: 1px solid rgba(167, 211, 224, 0.1);
    border-radius: 13px;
    background: rgba(4, 22, 37, 0.3);
  }

  .feature-icon {
    display: grid;
    flex: 0 0 31px;
    width: 31px;
    height: 31px;
    place-items: center;
    border-radius: 9px;
    font-size: 0.9rem;
  }

  .feature-icon.cyan {
    background: rgba(24, 213, 232, 0.12);
    color: #18d5e8;
  }

  .feature-icon.purple {
    background: rgba(124, 77, 255, 0.15);
    color: #a68cff;
  }

  .feature-icon.green {
    background: rgba(39, 209, 127, 0.12);
    color: #27d17f;
  }

  .feature-icon.orange {
    background: rgba(255, 166, 77, 0.12);
    color: #ffa64d;
  }

  .feature-card strong,
  .feature-card p {
    display: block;
  }

  .feature-card strong {
    font-size: 0.72rem;
  }

  .feature-card p {
    margin: 4px 0 0;
    color: #69899b;
    font-size: 0.61rem;
  }

  .feature-arrow {
    margin-left: auto;
    color: #55798d;
    font-size: 0.8rem;
  }

  .workspace-footer {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 12px;
    margin-top: 22px;
    padding-top: 18px;
    border-top: 1px solid rgba(167, 211, 224, 0.1);
    color: #7596a9;
    font-size: 0.68rem;
  }

  .workspace-footer div {
    display: flex;
    align-items: center;
    gap: 8px;
  }

  @media (max-width: 1150px) {
    .hero-container {
      grid-template-columns: 1fr;
      gap: 60px;
    }

    .hero-content {
      max-width: 800px;
    }

    .workspace-wrapper {
      max-width: 800px;
    }
  }

  @media (max-width: 650px) {
    .hero {
      padding: 140px 6% 70px;
    }

    h1 {
      font-size: 3rem;
    }

    .hero-description {
      font-size: 1rem;
    }

    .hero-stats {
      gap: 30px;
    }

    .stat strong {
      font-size: 2.4rem;
    }

    .workspace-body {
      grid-template-columns: 1fr;
    }

    .workspace-sidebar {
      display: none;
    }

    .dashboard {
      padding: 20px;
    }

    .workspace-header {
      padding: 20px;
    }

    .feature-grid {
      grid-template-columns: 1fr;
    }
  }
</style>