<section class="what-is-sxc" id="what-is-sxc">
  <div class="section-heading">
    <span class="eyebrow">WHAT IS SXC?</span>

    <h2>
      Build With Startups.<br />
      Create Real Products.
    </h2>

    <p>
      SintraX Creations is a student-powered environment where members work
      with startup teams, contribute to real projects, and gain practical
      product-building experience.
    </p>
  </div>

  <div class="info-grid">
    <article class="info-card">
      <span class="number">01</span>
      <h3>Startup Teams</h3>
      <p>
        Become part of a team working on a startup idea and help develop its
        product.
      </p>
    </article>

    <article class="info-card">
      <span class="number">02</span>
      <h3>Real Projects</h3>
      <p>
        Work on projects initiated by SXC or proposed by its members.
      </p>
    </article>

    <article class="info-card">
      <span class="number">03</span>
      <h3>Team Contribution</h3>
      <p>
        Take responsibility for meaningful tasks and contribute to your
        team's progress.
      </p>
    </article>

    <article class="info-card">
      <span class="number">04</span>
      <h3>Practical Experience</h3>
      <p>
        Learn through collaboration, problem-solving, and real product
        development.
      </p>
    </article>
  </div>
</section>

<style>
  .what-is-sxc {
    padding: 120px 6%;
    background: #ffffff;
    color: #071a2b;
  }

  .section-heading {
    max-width: 760px;
    margin-bottom: 55px;
  }

  .eyebrow {
    color: #168fd1;
    font-size: 0.8rem;
    font-weight: 700;
    letter-spacing: 0.18em;
  }

  h2 {
    margin: 18px 0;
    color: #071a2b;
    font-size: clamp(2.3rem, 5vw, 4.5rem);
    line-height: 1.05;
    letter-spacing: -0.04em;
  }

  .section-heading p {
    max-width: 650px;
    color: #5d7182;
    font-size: 1.05rem;
    line-height: 1.8;
  }

  .info-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 18px;
  }

  .info-card {
    min-height: 245px;
    padding: 30px;
    border: 1px solid #dce7ef;
    border-radius: 20px;
    background: #f7fafc;
    box-shadow: 0 10px 30px rgba(7, 26, 43, 0.04);
    transition:
      transform 0.25s ease,
      box-shadow 0.25s ease,
      border-color 0.25s ease;
  }

  .info-card:hover {
    transform: translateY(-5px);
    border-color: #b9dce9;
    box-shadow: 0 16px 35px rgba(7, 26, 43, 0.08);
  }

  .number {
    color: #168fd1;
    font-size: 0.85rem;
    font-weight: 700;
  }

  h3 {
    margin: 55px 0 18px;
    color: #071a2b;
    font-size: 1.35rem;
  }

  .info-card p {
    color: #617687;
    line-height: 1.7;
  }

  @media (max-width: 1000px) {
    .info-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 600px) {
    .what-is-sxc {
      padding: 80px 6%;
    }

    .section-heading {
      margin-bottom: 35px;
    }

    .info-grid {
      grid-template-columns: 1fr;
      gap: 15px;
    }

    .info-card {
      min-height: auto;
      padding: 25px;
    }

    h3 {
      margin: 35px 0 14px;
    }
  }
</style>