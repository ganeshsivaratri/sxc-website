<section class="benefits-hero">
  <div class="hero-grid"></div>
  <div class="glow glow-one"></div>
  <div class="glow glow-two"></div>

  <div class="hero-container">
    <div class="hero-content">
      <div class="eyebrow">
        <span class="eyebrow-dot"></span>
        THE SXC EXPERIENCE
      </div>

      <h1>
        More Than an Internship.
        <span>Build Projects.</span>
        <strong>Gain Real Experience.</strong>
      </h1>

      <p class="hero-description">
        Step into a practical project-building environment where you work
        with a team, solve meaningful problems, develop real products, and
        build experience that goes beyond a traditional internship.
      </p>

      <div class="hero-actions">
        <a href="/join" class="primary-button">
          Start Your Journey
          <span>↗</span>
        </a>

        <a href="#student-benefits" class="secondary-button">
          Explore Benefits
          <span>↓</span>
        </a>
      </div>

      <div class="hero-stats">
        <div class="stat-item">
          <strong>45</strong>
          <span>Days of Building</span>
        </div>

        <div class="stat-divider"></div>

        <div class="stat-item">
          <strong>3–6</strong>
          <span>Members per Team</span>
        </div>

        <div class="stat-divider"></div>

        <div class="stat-item">
          <strong>01</strong>
          <span>Real Project Experience</span>
        </div>
      </div>
    </div>

    <div class="hero-visual" aria-label="SXC project-building experience preview">
      <div class="visual-glow"></div>

      <div class="experience-card">
        <div class="card-topbar">
          <div class="window-controls">
            <span></span>
            <span></span>
            <span></span>
          </div>

          <div class="workspace-label">
            SXC / PROJECT EXPERIENCE
          </div>

          <div class="live-status">
            <span></span>
            LIVE
          </div>
        </div>

        <div class="card-body">
          <div class="workspace-sidebar">
            <div class="sidebar-logo">SXC</div>

            <div class="sidebar-menu">
              <div class="menu-item active">
                <span>▦</span>
                Workspace
              </div>

              <div class="menu-item">
                <span>◈</span>
                My Project
              </div>

              <div class="menu-item">
                <span>♧</span>
                My Team
              </div>

              <div class="menu-item">
                <span>✓</span>
                Progress
              </div>
            </div>

            <div class="sidebar-bottom">
              <span class="profile-circle">A</span>
              <div>
                <strong>Student Builder</strong>
                <small>SXC Member</small>
              </div>
            </div>
          </div>

          <div class="workspace-main">
            <div class="workspace-heading">
              <div>
                <span class="small-label">YOUR JOURNEY</span>
                <h2>From Idea to Impact</h2>
              </div>

              <span class="journey-badge">IN PROGRESS</span>
            </div>

            <div class="progress-section">
              <div class="progress-heading">
                <span>Project Progress</span>
                <strong>72%</strong>
              </div>

              <div class="progress-track">
                <div class="progress-fill"></div>
              </div>

              <div class="progress-meta">
                <span>Building in progress</span>
                <span>Day 32 of 45</span>
              </div>
            </div>

            <div class="journey-grid">
              <div class="journey-step completed">
                <span class="step-number">01</span>
                <div>
                  <strong>Understand</strong>
                  <small>Explore the problem</small>
                </div>
                <span class="step-check">✓</span>
              </div>

              <div class="journey-step completed">
                <span class="step-number">02</span>
                <div>
                  <strong>Design</strong>
                  <small>Plan the solution</small>
                </div>
                <span class="step-check">✓</span>
              </div>

              <div class="journey-step active-step">
                <span class="step-number">03</span>
                <div>
                  <strong>Build</strong>
                  <small>Create the product</small>
                </div>
                <span class="step-arrow">↗</span>
              </div>

              <div class="journey-step">
                <span class="step-number">04</span>
                <div>
                  <strong>Deliver</strong>
                  <small>Showcase your work</small>
                </div>
                <span class="step-arrow">→</span>
              </div>
            </div>

            <div class="workspace-footer">
              <div class="team-preview">
                <div class="avatar avatar-one">A</div>
                <div class="avatar avatar-two">R</div>
                <div class="avatar avatar-three">S</div>
                <div class="avatar avatar-four">K</div>
                <span>+2 teammates</span>
              </div>

              <div class="task-count">
                <span>✓</span>
                18 tasks completed
              </div>
            </div>
          </div>
        </div>
      </div>

      <div class="floating-note note-one">
        <span class="note-icon">✦</span>
        <div>
          <strong>Real Skills</strong>
          <small>Learn by building</small>
        </div>
      </div>

      <div class="floating-note note-two">
        <span class="note-icon">↗</span>
        <div>
          <strong>Team Growth</strong>
          <small>Collaborate & create</small>
        </div>
      </div>
    </div>
  </div>
</section>

<section class="benefits-intro" id="student-benefits">
  <div class="intro-container">
    <span class="section-label">WHY SXC</span>

    <h2>
      Experience that helps you
      <span>move forward.</span>
    </h2>

    <p>
      SXC helps students turn knowledge into practical experience through
      structured projects, teamwork, problem-solving, and meaningful
      product-building opportunities.
    </p>
  </div>
</section>

<style>
  :global(*) {
    box-sizing: border-box;
  }

  .benefits-hero {
    position: relative;
    isolation: isolate;
    overflow: hidden;
    min-height: 850px;
    padding: 150px 6% 100px;
    background: #071a2b;
    color: #ffffff;
  }

  .hero-grid {
    position: absolute;
    inset: 0;
    z-index: -3;
    opacity: 0.2;
    background-image:
      linear-gradient(rgba(24, 213, 232, 0.08) 1px, transparent 1px),
      linear-gradient(90deg, rgba(24, 213, 232, 0.08) 1px, transparent 1px);
    background-size: 55px 55px;
    mask-image: linear-gradient(to bottom, black, transparent);
  }

  .glow {
    position: absolute;
    z-index: -2;
    border-radius: 50%;
    filter: blur(100px);
    pointer-events: none;
  }

  .glow-one {
    top: 80px;
    left: -180px;
    width: 500px;
    height: 500px;
    background: rgba(24, 213, 232, 0.12);
  }

  .glow-two {
    right: -180px;
    bottom: 0;
    width: 600px;
    height: 600px;
    background: rgba(47, 128, 237, 0.12);
  }

  .hero-container {
    display: grid;
    grid-template-columns: 0.9fr 1.1fr;
    align-items: center;
    gap: 70px;
    max-width: 1450px;
    margin: 0 auto;
  }

  .hero-content {
    position: relative;
    z-index: 2;
  }

  .eyebrow {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    margin-bottom: 28px;
    color: #18d5e8;
    font-size: 0.75rem;
    font-weight: 700;
    letter-spacing: 0.2em;
  }

  .eyebrow-dot {
    width: 9px;
    height: 9px;
    border-radius: 50%;
    background: #18d5e8;
    box-shadow: 0 0 18px rgba(24, 213, 232, 0.8);
  }

  h1 {
    max-width: 650px;
    margin: 0;
    font-size: clamp(2.8rem, 4.4vw, 5rem);
    font-weight: 700;
    line-height: 1.08;
    letter-spacing: -0.055em;
  }

  h1 span {
    display: block;
    color: #18d5e8;
  }

  h1 strong {
    display: block;
    color: #ffffff;
    font-weight: 700;
  }

  .hero-description {
    max-width: 570px;
    margin: 30px 0 0;
    color: #a9bdcc;
    font-size: 1.05rem;
    line-height: 1.85;
  }

  .hero-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 15px;
    margin-top: 38px;
  }

  .primary-button,
  .secondary-button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 22px;
    min-height: 54px;
    padding: 0 22px;
    border-radius: 8px;
    font-size: 0.9rem;
    font-weight: 700;
    text-decoration: none;
    transition:
      transform 0.25s ease,
      background 0.25s ease,
      border-color 0.25s ease;
  }

  .primary-button {
    background: #18d5e8;
    color: #071a2b;
  }

  .primary-button:hover {
    transform: translateY(-3px);
    background: #65e8f4;
  }

  .secondary-button {
    border: 1px solid rgba(169, 189, 204, 0.3);
    color: #ffffff;
  }

  .secondary-button:hover {
    transform: translateY(-3px);
    border-color: #18d5e8;
  }

  .hero-stats {
    display: flex;
    align-items: center;
    gap: 24px;
    margin-top: 58px;
  }

  .stat-item {
    display: flex;
    flex-direction: column;
    gap: 8px;
  }

  .stat-item strong {
    color: #ffffff;
    font-size: 1.65rem;
    font-weight: 700;
  }

  .stat-item span {
    color: #91a9ba;
    font-size: 0.72rem;
    line-height: 1.4;
  }

  .stat-divider {
    width: 1px;
    height: 45px;
    background: rgba(169, 189, 204, 0.2);
  }

  .hero-visual {
    position: relative;
    min-width: 0;
    padding: 30px 0;
  }

  .visual-glow {
    position: absolute;
    inset: 10% 5%;
    z-index: -1;
    border-radius: 50%;
    background: rgba(24, 213, 232, 0.14);
    filter: blur(80px);
  }

  .experience-card {
    position: relative;
    overflow: hidden;
    border: 1px solid rgba(169, 189, 204, 0.24);
    border-radius: 18px;
    background: rgba(8, 29, 47, 0.92);
    box-shadow:
      0 35px 90px rgba(0, 0, 0, 0.35),
      0 0 0 1px rgba(24, 213, 232, 0.04);
    transform: perspective(1400px) rotateY(-4deg) rotateX(2deg);
  }

  .card-topbar {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 15px;
    min-height: 58px;
    padding: 0 22px;
    border-bottom: 1px solid rgba(169, 189, 204, 0.14);
    background: rgba(4, 18, 31, 0.6);
  }

  .window-controls {
    display: flex;
    gap: 7px;
  }

  .window-controls span {
    width: 9px;
    height: 9px;
    border-radius: 50%;
    background: #3e596b;
  }

  .workspace-label {
    color: #8ea7b8;
    font-size: 0.63rem;
    font-weight: 700;
    letter-spacing: 0.1em;
  }

  .live-status {
    display: flex;
    align-items: center;
    gap: 7px;
    color: #27d17f;
    font-size: 0.63rem;
    font-weight: 700;
  }

  .live-status span {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: #27d17f;
  }

  .card-body {
    display: grid;
    grid-template-columns: 155px 1fr;
    min-height: 445px;
  }

  .workspace-sidebar {
    display: flex;
    flex-direction: column;
    padding: 24px 16px;
    border-right: 1px solid rgba(169, 189, 204, 0.12);
    background: rgba(4, 18, 31, 0.38);
  }

  .sidebar-logo {
    margin-bottom: 45px;
    color: #18d5e8;
    font-size: 1.4rem;
    font-weight: 800;
    letter-spacing: -0.06em;
  }

  .sidebar-menu {
    display: flex;
    flex-direction: column;
    gap: 9px;
  }

  .menu-item {
    display: flex;
    align-items: center;
    gap: 10px;
    padding: 12px 10px;
    border-radius: 7px;
    color: #7792a5;
    font-size: 0.7rem;
  }

  .menu-item span {
    font-size: 0.9rem;
  }

  .menu-item.active {
    background: rgba(24, 213, 232, 0.12);
    color: #18d5e8;
  }

  .sidebar-bottom {
    display: flex;
    align-items: center;
    gap: 8px;
    margin-top: auto;
  }

  .profile-circle {
    display: grid;
    width: 29px;
    height: 29px;
    place-items: center;
    border-radius: 50%;
    background: #2f80ed;
    color: #ffffff;
    font-size: 0.7rem;
    font-weight: 700;
  }

  .sidebar-bottom strong,
  .sidebar-bottom small {
    display: block;
  }

  .sidebar-bottom strong {
    color: #c7d7e2;
    font-size: 0.58rem;
  }

  .sidebar-bottom small {
    margin-top: 3px;
    color: #668398;
    font-size: 0.52rem;
  }

  .workspace-main {
    padding: 30px;
  }

  .workspace-heading {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: 15px;
  }

  .small-label {
    color: #718da0;
    font-size: 0.58rem;
    font-weight: 700;
    letter-spacing: 0.14em;
  }

  .workspace-heading h2 {
    margin: 9px 0 0;
    color: #ffffff;
    font-size: 1.45rem;
    letter-spacing: -0.04em;
  }

  .journey-badge {
    padding: 7px 9px;
    border: 1px solid rgba(39, 209, 127, 0.3);
    border-radius: 5px;
    background: rgba(39, 209, 127, 0.08);
    color: #27d17f;
    font-size: 0.53rem;
    font-weight: 700;
    white-space: nowrap;
  }

  .progress-section {
    margin-top: 34px;
  }

  .progress-heading,
  .progress-meta {
    display: flex;
    justify-content: space-between;
    gap: 15px;
  }

  .progress-heading {
    color: #a8c0ce;
    font-size: 0.68rem;
  }

  .progress-heading strong {
    color: #18d5e8;
  }

  .progress-track {
    height: 8px;
    margin-top: 13px;
    overflow: hidden;
    border-radius: 20px;
    background: #17354b;
  }

  .progress-fill {
    width: 72%;
    height: 100%;
    border-radius: inherit;
    background: linear-gradient(90deg, #18d5e8, #2f80ed);
  }

  .progress-meta {
    margin-top: 10px;
    color: #607f94;
    font-size: 0.57rem;
  }

  .journey-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 12px;
    margin-top: 30px;
  }

  .journey-step {
    position: relative;
    min-height: 115px;
    padding: 15px;
    border: 1px solid rgba(169, 189, 204, 0.13);
    border-radius: 10px;
    background: rgba(4, 18, 31, 0.3);
  }

  .journey-step.active-step {
    border-color: rgba(24, 213, 232, 0.5);
    background: rgba(24, 213, 232, 0.07);
  }

  .step-number {
    display: block;
    margin-bottom: 18px;
    color: #58758a;
    font-size: 0.62rem;
    font-weight: 700;
  }

  .journey-step strong,
  .journey-step small {
    display: block;
  }

  .journey-step strong {
    color: #d4e3eb;
    font-size: 0.72rem;
  }

  .journey-step small {
    margin-top: 6px;
    color: #68859a;
    font-size: 0.58rem;
  }

  .step-check,
  .step-arrow {
    position: absolute;
    top: 14px;
    right: 14px;
    color: #27d17f;
    font-size: 0.7rem;
  }

  .step-arrow {
    color: #5d7e93;
  }

  .active-step .step-arrow {
    color: #18d5e8;
  }

  .workspace-footer {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 15px;
    margin-top: 30px;
    padding-top: 20px;
    border-top: 1px solid rgba(169, 189, 204, 0.12);
  }

  .team-preview {
    display: flex;
    align-items: center;
  }

  .avatar {
    display: grid;
    width: 28px;
    height: 28px;
    margin-left: -7px;
    place-items: center;
    border: 2px solid #081d2f;
    border-radius: 50%;
    color: #ffffff;
    font-size: 0.57rem;
    font-weight: 700;
  }

  .avatar:first-child {
    margin-left: 0;
  }

  .avatar-one {
    background: #2f80ed;
  }

  .avatar-two {
    background: #7c4dff;
  }

  .avatar-three {
    background: #27d17f;
  }

  .avatar-four {
    background: #e18a4d;
  }

  .team-preview > span {
    margin-left: 10px;
    color: #6e8ba0;
    font-size: 0.56rem;
  }

  .task-count {
    color: #8da7b8;
    font-size: 0.58rem;
  }

  .task-count span {
    margin-right: 5px;
    color: #27d17f;
  }

  .floating-note {
    position: absolute;
    display: flex;
    align-items: center;
    gap: 12px;
    padding: 14px 17px;
    border: 1px solid rgba(169, 189, 204, 0.2);
    border-radius: 11px;
    background: rgba(8, 29, 47, 0.94);
    box-shadow: 0 18px 40px rgba(0, 0, 0, 0.25);
    backdrop-filter: blur(15px);
  }

  .note-one {
    top: 0;
    right: -20px;
  }

  .note-two {
    bottom: 0;
    left: -35px;
  }

  .note-icon {
    display: grid;
    width: 35px;
    height: 35px;
    place-items: center;
    border-radius: 9px;
    background: rgba(24, 213, 232, 0.12);
    color: #18d5e8;
    font-size: 1rem;
  }

  .floating-note strong,
  .floating-note small {
    display: block;
  }

  .floating-note strong {
    color: #ffffff;
    font-size: 0.72rem;
  }

  .floating-note small {
    margin-top: 5px;
    color: #7895a9;
    font-size: 0.59rem;
  }

  .benefits-intro {
    padding: 110px 6%;
    background: #f7faff;
    color: #071a2b;
  }

  .intro-container {
    max-width: 850px;
    margin: 0 auto;
    text-align: center;
  }

  .section-label {
    color: #2f80ed;
    font-size: 0.75rem;
    font-weight: 800;
    letter-spacing: 0.18em;
  }

  .benefits-intro h2 {
    margin: 22px 0 20px;
    font-size: clamp(2.2rem, 4vw, 4rem);
    line-height: 1.1;
    letter-spacing: -0.05em;
  }

  .benefits-intro h2 span {
    display: block;
    color: #2f80ed;
  }

  .benefits-intro p {
    max-width: 680px;
    margin: 0 auto;
    color: #5f7587;
    font-size: 1rem;
    line-height: 1.85;
  }

  @media (max-width: 1200px) {
    .hero-container {
      grid-template-columns: 1fr;
      gap: 75px;
    }

    .hero-content {
      max-width: 850px;
      margin: 0 auto;
      text-align: center;
    }

    .hero-description {
      margin-right: auto;
      margin-left: auto;
    }

    .hero-actions,
    .hero-stats {
      justify-content: center;
    }

    .hero-visual {
      width: min(100%, 850px);
      margin: 0 auto;
    }
  }

  @media (max-width: 700px) {
    .benefits-hero {
      min-height: auto;
      padding: 125px 5% 75px;
    }

    h1 {
      font-size: clamp(2.5rem, 10vw, 4rem);
    }

    .hero-description {
      font-size: 0.95rem;
    }

    .hero-stats {
      flex-wrap: wrap;
      gap: 18px;
    }

    .stat-divider {
      display: none;
    }

    .experience-card {
      transform: none;
    }

    .card-topbar {
      padding: 0 14px;
    }

    .workspace-label {
      font-size: 0.5rem;
    }

    .card-body {
      grid-template-columns: 1fr;
    }

    .workspace-sidebar {
      display: none;
    }

    .workspace-main {
      padding: 22px;
    }

    .workspace-heading h2 {
      font-size: 1.2rem;
    }

    .journey-grid {
      grid-template-columns: 1fr;
    }

    .workspace-footer {
      align-items: flex-start;
      flex-direction: column;
    }

    .floating-note {
      position: relative;
      inset: auto;
      display: inline-flex;
      margin-top: 15px;
    }

    .note-one,
    .note-two {
      left: auto;
      right: auto;
      top: auto;
      bottom: auto;
    }

    .benefits-intro {
      padding: 80px 6%;
    }
  }

  @media (max-width: 430px) {
    .hero-actions {
      flex-direction: column;
    }

    .primary-button,
    .secondary-button {
      width: 100%;
    }

    .hero-stats {
      align-items: center;
      flex-direction: column;
    }

    .stat-item {
      align-items: center;
    }

    .workspace-main {
      padding: 18px;
    }

    .workspace-heading {
      flex-direction: column;
    }

    .journey-badge {
      align-self: flex-start;
    }
  }
</style>